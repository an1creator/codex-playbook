# CONTEXT INDEX

Line format:
`<path-prefix> -> <path-to-CONTEXT.md> -> <brief: purpose/invariants/risks>`

## Context Capture Settings
- Status: On | Off (default: On)
- On: if a task changes code/configs, update CONTEXT.md/CONTEXT_INDEX.md for affected key folders (based on `git diff --name-only`).
- Off: skip update context.

- ...
